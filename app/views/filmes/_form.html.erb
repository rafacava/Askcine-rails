<%= form_with(model: filme, local: true) do |form| %>
  <% if filme.errors.any? %>
    <div id="error_explanation">
      <h3><%= pluralize(filme.errors.count, "error") %> prohibited this filme from being saved:</h3>

      <ul>
      <% filme.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <div class="field">
    <%= form.label :nome %>
    <%= form.text_field :nome %>
  </div>

  <div class="field">
    <%= form.label :ano %>
    <%= form.number_field :ano %>
  </div>

  <div class="field">
    <%= form.label :diretor %>
    <%= form.text_field :diretor %>
  </div>

  <div class="field">
    <%= form.label :categoria %>
    <%= form.text_field :categoria %>
  </div>

  <div class="field">
    <%= form.label :sinopse %>
    <%= form.text_field :sinopse %>
  </div>

  <div class="field">
    <%= form.label :imagem %>
    <%= form.text_field :imagem %>
  </div>

  <div class="actions waves-effect waves-light black btn">
    <i class="material-icons left">check</i>
    <%= form.submit 'Confirmar Cadastro', :class => 'white-text' %>
  </div>
  
<% end %>
