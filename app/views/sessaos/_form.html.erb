<%= form_with(model: sessao, local: true) do |form| %>
  <% if sessao.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sessao.errors.count, "error") %> prohibited this sessao from being saved:</h2>

      <ul>
      <% sessao.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :dia %>
    <%= form.date_select :dia %>
  </div>

  <div class="field">
    <%= form.label :horario %>
    <%= form.time_select :horario %>
  </div>

  <div class="field">
    <%= form.label :status %>
    <%= form.check_box :status %>
  </div>

  <div class="field">
    <%= form.label :cinema_id %>
    <%= form.text_field :cinema_id %>
  </div>

  <div class="field">
    <%= form.label :filme_id %>
    <%= form.text_field :filme_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
